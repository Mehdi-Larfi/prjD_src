programme tirage au sort

DEBUT

entier taille;  //déclaration variable

saisir la valeur de taille;

création du tableau participant[taille];
Création du tableau nombre[taille] ;

for(entier i=0;i<taille;i++){   //boucle

	entier x=0;
	obtenir un nom et l'affecter la variable à participant[x]; 
	obtenir et affecter un entier à nombre[x] ;
	réinitialiser la zone de texte;
	x++;
	}
	
choisir une variable au hazard dans participant;

afficher Bravo 'participant[taille]' à gagné !!!;

enregister la variable dans une base de données;

réinitialiser les variable;

FIN
